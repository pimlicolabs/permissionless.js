---
"permissionless": major
---

`GetRequiredPrefundReturnType` is now `bigint`, what `getRequiredPrefund` returns. Its parameters type is the new `GetRequiredPrefundParameters` (0.x exported the parameters under the return-type name).
