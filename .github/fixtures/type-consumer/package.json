{
    "name": "type-consumer-fixture",
    "private": true,
    "type": "module",
    "description": "Type-checks the packed permissionless tarball the way consumers do (issue #522) and runs the package's type tests against it. The tarball and the TypeScript under test are installed at CI time by the package-types workflow job.",
    "devDependencies": {
        "typescript": "5.9.3",
        "viem": "3.0.0-next.10",
        "vitest": "2.1.9"
    }
}
